export default {
  HOME: '/',
  THANK_YOU: {
    path: '/thank_you',
    thunk: async (dispatch, getState) => {
      // console.log(getState())
      // Do work before loading
      // dispatch the result to the store.
    },
  },
}
